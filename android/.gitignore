# Gradle build outputs
.gradle/
build/
app/build/
local.properties

# Gradle wrapper — generated in CI on every run, not checked in.
gradlew
gradlew.bat
gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
.DS_Store

# Signing — never commit any real keystore
*.jks
*.keystore
