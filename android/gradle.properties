# Bigger daemon heap so gradle doesn't OOM on CI.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# AndroidX is required by every modern Android dep.
android.useAndroidX=true

# Keep intermediate build outputs to a reasonable size on CI.
org.gradle.parallel=true
org.gradle.caching=true
